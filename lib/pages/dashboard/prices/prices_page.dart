import 'package:flutter/material.dart';

class PricesPage extends StatelessWidget {
  const PricesPage({Key? key}) : super(key: key);
  static const route = '/prices';
  @override
  Widget build(BuildContext context) {
    return Text('Preços a vista');
  }
}
